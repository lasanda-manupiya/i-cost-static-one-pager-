# Custom logo

Place your logo file at:

- `assets/logo.png`

The site header and footer are already configured to use this file automatically.

Recommended dimensions:
- Header: roughly square, at least 56x56 px (displayed at 28x28)
- Footer: same source file reused (displayed at 24x24)

If the file is missing, the original dot-mark fallback icon is shown.
